# Data scaffold

This directory is reserved for database migrations and seed import code.

- `migrations/` will contain PostgreSQL/PostGIS schema changes.
- `seeds/` will contain the NYC Open Data and NYU building import.
- `tests/` will contain idempotency and mapping tests.

The data ticket owns these implementations. This scaffold contains no schema or seed logic.
