# Claude Code Instructions

Read and follow `AGENTS.md` before changing this repository.

When working inside a subdirectory, also follow its nearest `AGENTS.md`. `AGENTS.md` is the canonical instruction source; do not duplicate or override it here.
